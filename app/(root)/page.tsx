
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Home"
};

const HomePage = async () => {
    
    return (
        <>
        Home Page
        </>
    );
}

export default HomePage;